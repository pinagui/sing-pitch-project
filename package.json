{
  "name": "sing-pitch-project",
  "version": "1.0.0",
  "description": "Pitch Training App - Full Stack",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "python backend/main_deploy.py",
    "dev": "echo 'Use start.ps1 para desenvolvimento local'"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 